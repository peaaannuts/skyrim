# --- Node / TypeScript (sp-plugin) ---
node_modules/
sp-plugin/build/

# --- Native SKSE plugin build artifacts ---
build/
*.dll
*.pdb
*.exe
*.obj
*.lib
*.exp
*.ilk

# --- Local secrets (never commit; see CLAUDE.md) ---
pipeline/config.local.json

# --- CommonLibSSE-NG submodule contents (reference via .gitmodules) ---
native-plugin/lib/commonlibsse-ng/

# --- Editor / OS noise ---
.vs/
.vscode/
*.user
Thumbs.db
.DS_Store
